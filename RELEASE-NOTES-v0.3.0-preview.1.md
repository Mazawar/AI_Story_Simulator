# v0.3.0-preview.1 · 预览版发布说明

**发布日期**：2026-08-29　**包体**：约 50 MB（模型走游戏内下载）

## 这是什么

单机 AI 剧情模拟器的第一个可玩预览版。把任意剧本（游戏主持人设定文档）放进 `script/`，AI 通读后生成剧情配置（资源体系/面板/主线节拍/角色），你扮演剧本中的一个角色，系统按主线推动剧情，你在每个节拍做出选择、影响走向。

## 内置剧本

凡人修仙传 · 人界篇 / 剑来 · 开放世界 / 完美世界 V4.2 / 我独自升级 / 爱情公寓 / 末日余生

## 快速开始

1. 解压后双击 `AIStorySimulator.exe`（首次运行自动下载**主力模型 1.1GB**，或到设置页选择增强档 4B）；
2. 剧本架选一个剧本入局，完成开局向导；
3. 等待首次推演（本地 1.7B 约一两分钟，之后每轮数秒），剧情开始后用选项 / 「继续」/ 自由输入推演。

## 本版核心特性

- **AI 剧本配置**：通读任意剧本生成资源体系 / 面板 / 主线节拍 / 角色卡，无题材硬编码
- **剧情推动系统**：主线按"幕"推进（章节卡仪式感），模型宣告节拍完成自动进入下一幕
- **交互面板**：叙事内实体链接、右上角悬浮状态卡、剧本题材化的面板触发词、触发词快捷 chips
- **在线 API**：设置页配置任意 OpenAI 兼容接口（原生 JSON 模式、失败重试、连续失败自动回落本地）
- **双模型档位**：1.7B 主力（快）/ 4B 增强（质量），设置页一键下载切换
- **存档**：多槽位 + 自动进度保存，重开程序从"未竟之局"一键续玩
- **安全**：本地服务仅监听 127.0.0.1 + 一次性令牌；在线端点校验（SSRF 防护）

## 已知限制

- 本地 1.7B 档位的叙事质量与指令遵循有限，偶发推演失败会自动重试/回落；
  追求体验建议在设置页配置在线 API（如 DeepSeek）
- 剧本推演的解析失败详情记录在 `data/play_errors.log`
- 首次推演因剧本通读/模型加载有一次性等待

## 从源码构建

见 README.md（uv 工作流 + `uv run python build/make_exe.py`）。
